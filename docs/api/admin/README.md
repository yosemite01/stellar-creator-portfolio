# Domain: `admin`

Postman folder **admin** in `../collections/stellar-main-admin.postman_collection.json`.

Includes: pending reviews, moderation, referral admin actions (requires **ADMIN** session).
